import Foundation
import SwiftData

// Schema versioning disabled — model classes have evolved beyond the frozen V1 schema.
// The app uses a flat schema via ModelConfiguration in MyAIssistantApp.init().
